#include <iostream> 
using namespace std; 

int main() {
        // The constant definition goes before the variable type and name	
	const int HEIGHT = 20; 
	const int WIDTH = 4; 
	const char NEWLINE = '\n'; 
	int area; 
	
	area = HEIGHT * WIDTH; 
	cout << area << NEWLINE; 
	return 0; 
} 
